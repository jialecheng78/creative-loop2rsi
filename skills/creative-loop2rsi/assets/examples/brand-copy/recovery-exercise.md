# L2 局部恢复演练（示意，不是执行证据）

1. 封存一次通过硬合同的 attempt。
2. 模拟评价 JSON 缺失一个闭合括号，但不修改文案正文。
3. 执行确定性 JSON 修复，生成新的评价文件和 manifest 条目。
4. 核对文案正文哈希、brief 哈希和旧 attempt 均未改变。
5. 若无法证明修复不改变创作语义，停止并创建新 attempt。

只有真实运行目录、哈希和发布门结果齐全后，才能把恢复演练计入 L2 证据。
