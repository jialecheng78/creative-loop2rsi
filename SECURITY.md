# 安全政策

## 支持范围

在首个稳定标签发布前，`main` 是预发布代码，只接受尽力而为的安全修复。发布后，安全修复优先覆盖最新版本；旧版本是否回补会在公告中说明。

以下问题属于本项目的安全范围：

- 初始化覆盖非空目录、越界写入或路径穿越；
- 已封存 attempt 可被静默改写；
- 候选绕过受保护表面、人工审批或 L5 禁止自动晋升规则；
- 私密素材、凭证或本机路径被意外打包进公开树；
- GitHub Actions 获得超出只读源码所需的权限。

模型输出的文学质量、作品市场表现以及第三方 Codex/操作系统漏洞不属于本仓库的安全保证，但仍欢迎提交可复现的边界问题。

## 私下报告

请使用 GitHub 仓库 **Security -> Report a vulnerability** 创建私密报告。不要在公开 issue、讨论区或示例中粘贴漏洞利用代码、未发表作品、账号或密钥。

报告尽量包含：

- 受影响版本或 commit；
- 最小复现步骤和预期/实际结果；
- 可能受影响的文件和安全边界；
- 已做的临时缓解措施；
- 不含真实敏感内容的虚构复现材料。

如果仓库尚未启用私密报告，请等待维护者提供私密渠道；不要为了联系方便而公开敏感细节。

## 使用者的隐私责任

- `loopctl.py` 不联网，但它会在你指定的位置写入项目状态和证据。运行前检查目标路径和文件权限。
- 生成项目会忽略常见输入、输出和运行轨迹，但 `.gitignore` 不是访问控制。提交前仍应检查 `git status` 和公开树审计结果。
- attempt 哈希用于发现意外或事后改写，不是加密签名，也不能抵御拥有本机写权限的攻击者。
- `approved_by` 是可审计的本地声明，不是身份认证或电子签名。高风险项目应在外部权限系统中完成审批，并把只读审批回执作为证据导入。
- 不要把真实密钥放进 Prompt、创作宪法、finding、测试、issue 或候选说明。
- 不要把本工具当作运行不可信代码的沙箱。

如果凭证已经进入 Git 历史，仅删除当前文件不够：应先撤销并轮换凭证，再清理历史，并在公开前复核完整历史。
